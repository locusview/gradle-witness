apply plugin: 'groovy'

dependencies {
    implementation gradleApi()
    implementation localGroovy()
}

def buildNumber = System.getenv("BUILD_NUMBER") ?: '3'
version = "1.2.$buildNumber"

sourceCompatibility = '1.7'
targetCompatibility = '1.7'

